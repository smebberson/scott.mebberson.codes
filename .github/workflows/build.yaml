name: Build
on: [push]

jobs:
    build:
        name: Build
        runs-on: ubuntu-latest
        steps:
            - name: Setup
              uses: actions/setup
            - name: Build
              uses: actions/next
